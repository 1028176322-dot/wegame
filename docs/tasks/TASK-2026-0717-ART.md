<!-- 编码: UTF-8 -->
# TASK 2026-0717-ART — 从策划案提取美术资源清单

Status: completed
Risk: C1
Owner: Executor（橘子）
Reviewer: Decision Owner（肖俊）

## Goal
扫描 `docs/design` 全量设计文档（GDD + S01–S33 系统/平衡文档 + UX_CONTENT_BASELINE + SPEC_STANDARD），提取所有视觉美术资源需求，汇总为设计态资源清单：
- `docs/design/美术资源清单.md`（人类 + AI 可读主清单，8 节）
- `docs/design/art_resource_inventory.json`（机器可读，81 条 / 8 类）

## Non-Goals
- 不生产/生成任何实际美术资产（生产须走 art-pipeline 技能 + 人工确认节点）
- 不修改任何 frozen 文档（canonical / 冻结锁 10 文档）
- 不改动游戏代码或配置 schema
- 音频资源仅单列参考，不计入美术清单主体

## Authoritative Sources
- `docs/design/GDD.md`（塔种、主题配色）
- `docs/design/systems/S01..S33`（各系统 §4 美术资源需求表格）
- `docs/design/balance/S01..S33`（平衡文档，资产规模佐证）
- `docs/design/UX_CONTENT_BASELINE.md`（ux-content-assets current 基线）
- `docs/design/SPEC_STANDARD.md`（§4 资产规格：单图≤128KB、合图集、@2x/@3x、SDF 字体）
- `config/file_placement_rules.json`（放置合法性）
- `config/root_allowlist.json`（根目录允许清单）

## Preconditions
- 仓库已 clone 至 `E:\wegame`，main 已拉取最新
- Phase 5 检查器 `tools/validators/` 已 verified 且可执行

## Dependencies
- 无外部依赖；纯文档提取

## Impact Map
- affected：`docs/design/`（新增 2 文件，非冻结锁）
- not affected：governance canonical、冻结锁 10 文档、游戏代码、data/ 配置
- unassessed：S31 敌人实体未建，清单中 enemy/Boss 美术为 provisional 占位契约

## File Claims
- NEW `docs/design/美术资源清单.md`
- NEW `docs/design/art_resource_inventory.json`

## Allowed Changes
- 仅在 `docs/design/` 新增上述 2 文件

## Forbidden Scope
- 不改 canonical / 冻结锁文档
- 不标 accepted（仅 Decision Owner 可标）
- 不 push（push 须 DO 授权）

## Baseline SHA-256
- HEAD 前基线：`003865a`（A→B→C accepted 推送后 HEAD）
- 本任务落地提交：`64305f7`
- 注：`docs/design/` 不在冻结锁 10 文档内，无需逐文件 SHA 冻结；门禁仅校验放置/秘密/冻结锁一致性。

## Failing Evidence
- 无阻断性失败；上一轮执行时 `run_validators.py` 返回 0 ERROR（放置 0/0、秘密 0、冻结锁一致），JSON 合法（81 items）。

## Implementation Steps
1. 扫描 `docs/design` 全量 md，grep 美术关键词（立绘/图标/精灵/背景/特效/字体…）
2. 汇总各系统 §4 美术资源需求表格 → 分类（背景/塔/敌人Boss/状态特效/UI/引导/字体/音频参考）
3. 写主清单 md（8 节：总览/分类明细/规格预算/分包/人工确认/缺口/JSON 骨架说明/音频参考）
4. 写 machine-readable JSON（字段 id/name/category/spec/source_system/subpackage/reuse/status/priority）
5. 跑 `run_validators.py` 确认治理合规
6. 本地提交 `64305f7`（未 push）

## Validation Matrix
| 项 | 期望 | 实际 |
|---|---|---|
| 文件放置合法 | `docs/design/` 在 allowlist | ✅ PASS |
| 门禁 | `run_validators` 0 ERROR | ✅（执行时 0 ERROR） |
| JSON 合法 | parse 成功，81 items | ✅ PASS |
| 冻结锁 | 未触碰 10 文档 | ✅ PASS |
| 不 push | 本地领先 origin 1 | ✅ |

## Rollback
- `git revert 64305f7` 或 `git reset --soft 003865a`（纯新增文件，安全）

## Stop Conditions
- 门禁 FAIL / 秘密命中 / 冻结锁漂移 / 误改 canonical → 立即停止

## Report / Handoff
- 本任务已于 2026-07-17 本地提交 `64305f7`，状态 completed；按红线未自标 accepted，待 Decision Owner 复核裁定。
- 注：本任务卡为**事后补录**——原执行时遗漏 §21.16 启动回执与任务卡/CL 落盘，现补正以符合治理要求。

---

### 启动合规回执（§21.16，回溯补录）
- Task ID: 2026-0717-ART
- Role: Executor（冻结范围内实施 + 局部验证）；Reviewer = Decision Owner 独立复核
- Risk: C1（docs/design 新增文档，非冻结锁、非代码、可逆）
- Rules read: PROJECT_RULES / AUTHORITY_INDEX / 通用AI项目管理方案(canonical) / file_placement_rules.json / root_allowlist.json / UX_CONTENT_BASELINE / SPEC_STANDARD
- Applicable current: ux-content-assets(current, UX_CONTENT_BASELINE); product-design(GDD); system-design; data-config; 其余 current 入口
- NEW: `docs/design/美术资源清单.md`, `docs/design/art_resource_inventory.json`
- MOD: 无
- VERIFY: 冻结锁 10 文档未变；`run_validators` 全绿
- Forbidden: 不改 canonical/冻结锁；不标 accepted；不 push
- Baseline SHA: HEAD 前 `003865a`
- Multi-writer: 唯一 Writer，串行
- Validation: `run_validators.py` 0 ERROR；JSON 合法
- Git plan: 本地提交，不 push（push 须 DO 授权）
- Stop: 门禁 FAIL / 秘密命中 / 冻结锁漂移
