<!-- 编码: UTF-8 -->
# TASK 2026-0717-GOV — 落实项目管理规则（Phase 5 检查器 + 进度对齐 + 裁定未定域）

Status: accepted
Risk: C3
Owner: Decision Owner
Reviewer: Decision Owner（独立复核；Executor 不得自标 accepted）

## Goal
1. 实现 Phase 5 工程/安全/门禁检查器 `tools/validators/`，把 `TEST_COMMAND`/`VALIDATE_COMMAND` 从 `NOT_IMPLEMENTED` 转为可执行。
2. 收口 `docs/progress/_index.md` 业务进度段，与 `AUTHORITY_INDEX` 已裁定 current 对齐。
3. 裁定 2 个仍 `unassessed` 的域（ux-content-assets、test-quality）的 current 入口并补冻结锁。

## Non-Goals
- 不修改方法论 canonical 正文。
- 不降级任何治理要求。
- 不实现游戏引擎/业务代码（仅治理执行层）。
- 不 push 到远程（需 Decision Owner 单独授权）。

## Authoritative Sources
- `docs/governance/通用AI项目管理方案.md`（冻结 SHA `0bb4b00b…`）
- `docs/governance/ENGINEERING_RULES.md`、`PROGRESS_RULES.md`、`SECURITY_RULES.md`
- `config/root_allowlist.json`、`config/file_placement_rules.json`
- `docs/governance/AUTHORITY_INDEX.md`（冻结锁表）

## Preconditions
- HEAD = `050d44f`；工作区干净（无未跟踪冲突）。
- 检查器需 Python 3（仓库 `tools/generators` 同为 Python，沿用）。

## Dependencies
- 无外部依赖；纯标准库 + `git`（只读 `git ls-files`）。
- `config/commands.json` 作为 `TEST_COMMAND`/`VALIDATE_COMMAND` 的机器可读定义。

## Impact Map
- affected：`tools/validators/*`（新增）、`config/commands.json`（新增）、`ENGINEERING_RULES.md`（状态变更）、`docs/progress/_index.md`（Phase5+业务进度）、`config/file_placement_rules.json`（补登子目录）、`AUTHORITY_INDEX.md`（裁定2域+冻结锁）、Task C 两个基线 spec。
- not affected：方法论 canonical、TECH_ARCHITECTURE/PLATFORM_RELEASE/GDD 等已冻结 current 文档内容。
- unassessed：工具运行时的跨平台路径边角（已在脚本内按 OS 归一化，待真实多平台验收 E4）。

## File Claims
- Writer 唯一；AUTHORITY_INDEX.md 与 _index.md 为热文件，全程单 Writer 串行处理。

## Allowed Changes
- 见 Change List `CL-2026-0717-gov.md` 的 NEW / MOD 清单。

## Forbidden Scope
- 不改 canonical 正文；不降治理要求；不顺手改范围外 current 文档。
- 不标 `accepted`（仅 Decision Owner 可标）。
- 不 commit / push / force-push / 重写历史（commit 为本地、按任务分提交；push 需单独授权）。

## Baseline SHA-256（预编辑）
| 文件 | SHA-256 |
|---|---|
| `docs/governance/ENGINEERING_RULES.md` | `2441bd750dac3190f6dd275f8e80ffe90b5725108115445c451f16f995e8c4af` |
| `docs/progress/_index.md` | `f6bf865915f8c882266c95da19f3670224b9925521ccf8066f918e53f7d4bc43` |
| `config/file_placement_rules.json` | `0d966bd1d738600d2b9e86b1dc54ce108d5c3a13fc69a9bf3cee4d6d9a8d1f90` |
| `docs/governance/AUTHORITY_INDEX.md` | `96546ced5d99e7bc9864aa5f4526cbcec1f426159dec126f8c732b5753a8d947` |

## Failing Evidence
- 当前 `TEST_COMMAND`/`VALIDATE_COMMAND` 为 `NOT_IMPLEMENTED`（ENGINEERING_RULES §17.3），Phase 5 = `in_progress`。
- `tools/validators/` 不存在（仅 `tools/generators/`）。

## Implementation Steps
1. 写 `tools/validators/file_placement_validator.py`：依据 root_allowlist + file_placement_rules 校验根文件/顶层目录/子目录模式；全局禁名检查；返回明确退出码。
2. 写 `tools/validators/secret_validator.py`：扫描密钥/私钥/高熵串；输出脱敏（规则ID/路径/行号/不可逆指纹）；返回退出码。
3. 写 `tools/validators/gate_validator.py`：校验 AUTHORITY_INDEX 冻结锁 SHA 一致性 + 进度七值/五状态词合法性。
4. 写 `tools/validators/run_validators.py`（VALIDATE_COMMAND 入口）汇总三者。
5. 写 `tools/validators/run_tests.py`（TEST_COMMAND 入口）含负向 fixture 自测。
6. 写 `config/commands.json` 记录两个命令。
7. MOD `ENGINEERING_RULES.md` 第23行→实际命令 + 证据 E3。
8. MOD `config/file_placement_rules.json` 补登现有子目录。
9. MOD `docs/progress/_index.md`：Phase5→verified；业务进度与 AUTHORITY_INDEX 对齐。
10. Task C：建两个基线 spec；MOD `AUTHORITY_INDEX.md` 裁定 current + 冻结锁。
11. 运行 `run_tests.py` 与 `run_validators.py` 验证（E3）。
12. 按任务本地提交；不 push。

## Validation Matrix
| 检查 | 方法 | 通过条件 |
|---|---|---|
| 文件放置 | `run_validators.py` | 当前仓库 0 ERROR |
| 秘密 | `secret_validator.py` | 0 命中 |
| 冻结锁 | `gate_validator.py` | 8 文档 SHA 一致 |
| 进度合法 | `gate_validator.py` | 七值/五状态词无非法 |
| 负向 fixture | `run_tests.py` | 全部 PASS，无 FAIL |

## Rollback
- `git restore` 各 MOD 文件至 Baseline SHA；删除 NEW 文件；`git revert` 对应提交。

## Stop Conditions
- 任一冻结 SHA 漂移；run_tests 出现 FAIL；run_validators 出现 ERROR；秘密命中；同域 >1 current 冲突。

## Report / Handoff
- 完成后输出完成回执（§21.3），交由 Decision Owner 独立复核并标 `accepted`。

## Acceptance（Decision Owner 裁定，2026-07-17 21:40 GMT+8）
- Verdict: **accepted**（依 Decision Owner 指令落档；Executor 未自标）
- Decision Owner / Reviewer：肖俊（human-AI 模式下独立复核并裁定接受）
- Evidence: **E3**（真实环境 + 远程验收）
  - `run_tests.py`：13/13 PASS（负向 fixture 检测能力验证）
  - `run_validators.py`：0 ERROR（放置 0 error / 0 warning；秘密 0 命中；冻结锁 10 文档 SHA 一致 + 进度七值/五状态词合法）
  - Git：main 已推送 origin/main（`050d44f..965074a`），本地 = 远程；无未推送提交
- Impact：Phase 5 检查器由 `NOT_IMPLEMENTED` → 可执行（成熟度 L2→L3）；Phase 5 = verified；业务进度与 `AUTHORITY_INDEX` 对齐；`ux-content-assets` / `test-quality` 裁定为 current 且冻结锁一致
- 随任务一并 accepted：Task C 两份基线 spec（`docs/design/UX_CONTENT_BASELINE.md`、`docs/decisions/ADR-0004-test-quality-baseline.md`）作为各自域 current 入口生效
- 未解决风险：无阻断项；两份基线 spec 为 v0.1，后续工程任务可迭代，但已 accepted 作为当前 baseline
- 备注：本任务原 Non-Goal「不 push」已由 DO 于 2026-07-17 19:55 授权（".workbuddy 须入库到远程"），accepted 落档随治理文书一并推送
