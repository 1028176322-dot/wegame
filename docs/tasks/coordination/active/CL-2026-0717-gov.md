<!-- 编码: UTF-8 -->
# CHANGE LIST — CL-2026-0717-gov

Status: execution_authorized
Risk: C3
Authorization mode: Decision Owner 直接裁定（chat 2026-07-17「abc依次完成」）
Task-card SHA: `docs/tasks/TASK-2026-0717-GOV.md` 新建（无预编辑 SHA）

## Decisions
- D-06 落实 Phase 5：以 `tools/validators/` 三件套 + 命令配置将 `TEST_COMMAND`/`VALIDATE_COMMAND` 由 `NOT_IMPLEMENTED` 转可执行。
- D-07 进度对齐：业务进度段与 AUTHORITY_INDEX 已裁定 current 对齐（产品/技术架构已是 current）。
- D-08 裁定 ux-content-assets / test-quality 的 current 入口（建基线 spec，owner=Decision Owner）。

## NEW
- `tools/validators/file_placement_validator.py`
- `tools/validators/secret_validator.py`
- `tools/validators/gate_validator.py`
- `tools/validators/run_validators.py`（VALIDATE_COMMAND 入口）
- `tools/validators/run_tests.py`（TEST_COMMAND 入口）
- `tools/validators/README.md`
- `config/commands.json`（TEST_COMMAND / VALIDATE_COMMAND 机器可读定义）
- `docs/tasks/TASK-2026-0717-GOV.md`
- Task C：`docs/design/UX_CONTENT_BASELINE.md`（ux-content-assets current 入口）
- Task C：`docs/decisions/ADR-0004-test-quality-baseline.md`（test-quality current 入口）

## MOD
- `docs/governance/ENGINEERING_RULES.md`：§17.3 `TEST_COMMAND`/`VALIDATE_COMMAND` 由 `NOT_IMPLEMENTED` 改为实际命令 + 证据 E3。
- `docs/progress/_index.md`：治理落地 Phase 5 行 `in_progress`→`verified`；业务进度段与 AUTHORITY_INDEX 对齐（product/tech/ux/test 均 current）。
- `config/file_placement_rules.json`：补登现有合法子目录（docs/design、config、data 及 docs/tasks/templates、docs/tasks/coordination），最长前缀匹配。
- `docs/governance/AUTHORITY_INDEX.md`：ux-content-assets / test-quality 由 `unassessed`→`current`；末注更新；冻结锁补 2 张 new current 文档 SHA；**并校正 3 处漂移 SHA**（GDD/FEATURE_SCOPE/SYSTEM_BREAKDOWN——最新 pull 改了文档但未更新锁，本次一并校正）。
- `.gitignore`：新增 `.workbuddy/`（本地工具运行目录，非项目源码，取消跟踪）。

## VERIFY / NOCHG
- 冻结锁 8 文档（canonical / TECH / PLATFORM / GDD / SYSTEM_BREAKDOWN / FEATURE_SCOPE / GAME_FLOW / data README）SHA 不变，仅由 gate_validator 校验。
- 其他 current 文档内容不变。

## DELETE / MOVE / RENAME
- 无

## Forbidden Scope
- 不得修改方法论 canonical 文档正文。
- 不得在此变更中顺手降低治理要求或改写其他 current 文档。
- 不得 commit / push（推送需 Decision Owner 单独授权）；不 force-push、不重写历史。
- Executor 不得自行标 `accepted`。

## Baseline SHA-256（预编辑）
| 文件 | SHA-256 |
|---|---|
| `docs/governance/ENGINEERING_RULES.md` | `2441bd750dac3190f6dd275f8e80ffe90b5725108115445c451f16f995e8c4af` |
| `docs/progress/_index.md` | `f6bf865915f8c882266c95da19f3670224b9925521ccf8066f918e53f7d4bc43` |
| `config/file_placement_rules.json` | `0d966bd1d738600d2b9e86b1dc54ce108d5c3a13fc69a9bf3cee4d6d9a8d1f90` |
| `docs/governance/AUTHORITY_INDEX.md` | `96546ced5d99e7bc9864aa5f4526cbcec1f426159dec126f8c732b5753a8d947` |

## Exact Instructions
1. 新建 `tools/validators/` 各脚本（纯标准库 + 只读 `git ls-files`），退出码明确。
2. 新建 `config/commands.json` 记录 TEST/VALIDATE 命令。
3. 编辑 ENGINEERING_RULES.md / _index.md / file_placement_rules.json / AUTHORITY_INDEX.md，保持表格结构。
4. Task C 新建两份基线 spec，写入后计算 SHA 追加至冻结锁。
5. 运行 `run_tests.py`（预期 PASS）与 `run_validators.py`（预期 0 ERROR）。
6. `git add` 指定文件，按任务本地提交（不推送）。

## Validation
- [ ] `python tools/validators/run_tests.py` 全部 PASS，退出码 0
- [ ] `python tools/validators/run_validators.py` 0 ERROR，退出码 0
- [ ] `gate_validator` 校验 8 冻结 SHA 一致
- [ ] ENGINEERING_RULES §17.3 状态为实际命令 + E3
- [ ] _index Phase5=verified；业务进度与 AUTHORITY_INDEX 对齐
- [ ] file_placement_rules 含补登子目录
- [ ] AUTHORITY_INDEX 两域 current + 冻结锁新增 SHA 一致
- [ ] 未触发 forbidden scope

## Rollback
- `git revert <commit>` 回退；或 `git restore` 各 MOD 文件至 Baseline SHA 并删除 NEW 文件。

## Authorization Invalidators
- Decision Owner 撤回裁定。
- 检查器在真实 CI 中出现误报导致门禁不可用（须先修再落地）。

## DoD
- Phase 5 检查器可执行且通过；进度索引与权威索引一致；两未定域裁定 current 且冻结锁一致；本地提交完成、未推送。

<!-- Change List 正文不自写自身最终 SHA；外部授权回执提供冻结锁。 -->

## Correction — D-09（2026-07-17，DO 裁定，19:55）
- **撤销原第31行 `.gitignore` 忽略 `.workbuddy/` 的处置**：DO 明确 `.workbuddy` 为项目治理运行目录（memory/skills/协调文件），须纳入版本控制并提交远程（不进游戏工程子目录即可）。原第31行作废。
- MOD（追加，均非冻结锁文件）：
  - `.gitignore`：移除 `.workbuddy/` 忽略行（保留磁盘文件，仅取消忽略；改注释说明须入库）。
  - `config/root_allowlist.json`：`allowedDirectories` 增加 `.workbuddy`。
  - `config/file_placement_rules.json`：登记 `.workbuddy` 放置规则（最长前缀覆盖子目录，消除 WARN）。
  - NEW 追踪：`.workbuddy/{memory/2026-07-17.md, memory/MEMORY.md, skills/project-governance/SKILL.md}`。
- 验证：`run_validators.py` 全绿（放置 0 error / 0 warning；secret 0 命中；冻结锁一致）。
- Git：本地提交 de83580 + 推送 origin/main（050d44f..de83580，含之前领先的 3 提交）。推送由 HTTPS 凭据缺失阻断 → 远程 URL 改回 SSH（对齐项目规范 origin `git@github.com:1028176322-dot/wegame.git`）。
- Risk：C2（非冻结锁文件改动）；审查：已随 TASK-2026-0717-GOV 由 DO 裁定 accepted（D-10）。

## Acceptance（DO 裁定，2026-07-17 21:40 GMT+8）
- TASK-2026-0717-GOV：`verification_pending` → **accepted**（Decision Owner 肖俊 依指令落档；Executor 未自标）。
- 证据 E3：`run_tests.py` 13/13 PASS；`run_validators.py` 0 ERROR（放置 0/0、secret 0、冻结锁 10 文档一致）；main 已推送 origin/main（`050d44f..965074a`）。
- 随任务 accepted：Task C 两基线 spec 作为各自域 current 入口生效（已在 AUTHORITY_INDEX 标 current + 冻结锁）。
- 风险：无阻断项；两 spec v0.1 后续工程任务可迭代，但已 accepted 作为当前 baseline。
- 范围：仅更新任务卡 `Status` + 本 CL 追加；不触碰冻结锁（docs/tasks 不在冻结锁内）。
