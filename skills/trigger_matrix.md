<!-- 编码: UTF-8 -->
# Skill 触发矩阵（current）

> 依据方案 §13.2、§13.4。至少含正向、负向、冲突优先级、fallback 与 stop 条件。

## 说明

当前已登记 1 个项目级 Skill：`project-governance`（状态 `active`，已通过 §13.5 新建准入与 §13.7 安全审查；来源/安装 hash 一致，`trust=locked`）。新增 Skill 须满足方案 §13.5 准入：明确重复工作、清晰触发条件、确定性脚本、至少三条正向+三条负向样例、安全审查、版本与回滚。

| Skill ID | 正向触发 | 负向触发（不触发） | 冲突优先级 | Fallback | Stop 条件 |
|---|---|---|---|---|---|
| project-governance | 创建/修改任务卡、需求、变更、发布、验证、复盘；任何写入磁盘的实施动作之前；用户提及"按方案/管理项目/任务卡/冻结/验证/发布"；跨会话恢复/交接/记忆沉淀 | 纯问答无磁盘写入；仅读取类查询且不改治理文件；与方案无关的普通聊天 | 治理类动作最高优先级，覆盖通用默认 | Skill 不可用时，回退为先读 `PROJECT_RULES.md` 与 `AUTHORITY_INDEX.md` 再手动按 §8 生命周期执行 | 内容 hash 漂移 → `quarantined`；用户要求绕过核心约束且无 Decision Owner 显式裁决 |

## 规则要点

- Skill 不能扩大任务授权；高风险权限每次任务重新取得。
- 安装 hash 漂移 → `quarantined` 并停止调用。
- 多个 Skill 同时匹配且无法裁决时，不自动组合。
